const { describe, it } = require('node:test')
const assert = require('node:assert')

describe('services/cat.js', () => {
  describe('getCat()', () => {
    it('should fetch a cat', () => {
      assert.ok(false)
    })
    it('should return not found if the cat does not exist', () => {
      assert.ok(false)
    })
  })
  describe('addCat()', () => {
    it('should add a cat', () => {
      assert.ok(false)
    })
    it('should update a cat if it already exist', () => {
      assert.ok(false)
    })
  })
  describe('watchCats()', () => {
    it('should notify when a cat is added', () => {
      assert.ok(false)
    })
    it('should notify when a cat is changed', () => {
      assert.ok(false)
    })
    it('should notify when a cat is removed', () => {
      assert.ok(false)
    })
  })
})
