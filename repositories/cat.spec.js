const { describe, it } = require('node:test')
const assert = require('node:assert')

describe('repositories/cat.js', () => {
  describe('addCat()', () => {
    it('should add a cat', () => {
      assert.ok(false)
    })
  })
  describe('removeCat()', () => {
    it('should add a cat', () => {
      assert.ok(false)
    })
  })
  describe('changes()', () => {
    it('should provide a subscription event emitter', () => {
      assert.ok(false)
    })
  })
})
